// types.dart
enum EventType {
  lecture,
  tutorial,
  lab,
  exam,
  other,
}

enum RecurrenceRule {
  none,
  daily, // lowercase
  weekly, // lowercase
  monthly, // lowercase
  custom, // lowercase
}
